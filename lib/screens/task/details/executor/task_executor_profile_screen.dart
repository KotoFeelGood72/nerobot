import 'package:flutter/material.dart';

class TaskExecutorProfile extends StatelessWidget {
  const TaskExecutorProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}